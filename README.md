git-metrics
==============

A collection of git extensions to report useful repository statistics
all within your terminal.

### Commands

##### `git summary`
##### `git churn`
##### `git authors`
##### `git history`
##### `git calendar`
##### `git history` 

### Options
  ```-v / --verbose```

  ```-o / --output```

